"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
const { dirname, normalize, resolve } = require('path');
const { nodeFileTrace } = require('@vercel/nft');
const { getBasePath } = require('../../utils/base_path');
const { filterExcludedPaths, getPathsOfIncludedFiles } = require('../../utils/included_files');
const bundle = ({ basePath, config, featureFlags, mainFile, name, pluginsModulesPath, repositoryRoot = basePath, srcDir, srcPath, stat, }) => __awaiter(void 0, void 0, void 0, function* () {
    const srcFiles = yield getSrcFiles({
        basePath: repositoryRoot,
        config: Object.assign(Object.assign({}, config), { includedFilesBasePath: config.includedFilesBasePath || basePath }),
        featureFlags,
        mainFile,
        name,
        pluginsModulesPath,
        repositoryRoot,
        srcDir,
        srcPath,
        stat,
    });
    const dirnames = srcFiles.map((filePath) => normalize(dirname(filePath))).sort();
    return {
        basePath: getBasePath(dirnames),
        inputs: srcFiles,
        mainFile,
        srcFiles,
    };
});
const getSrcFiles = function ({ basePath, config, mainFile }) {
    return __awaiter(this, void 0, void 0, function* () {
        const { includedFiles = [], includedFilesBasePath } = config;
        const { exclude: excludedPaths, paths: includedFilePaths } = yield getPathsOfIncludedFiles(includedFiles, includedFilesBasePath);
        const { fileList: dependencyPaths } = yield nodeFileTrace([mainFile], { base: basePath });
        const normalizedDependencyPaths = dependencyPaths.map((path) => (basePath ? resolve(basePath, path) : resolve(path)));
        const includedPaths = filterExcludedPaths([...normalizedDependencyPaths, ...includedFilePaths], excludedPaths);
        return includedPaths;
    });
};
module.exports = { bundle, getSrcFiles };
//# sourceMappingURL=index.js.map