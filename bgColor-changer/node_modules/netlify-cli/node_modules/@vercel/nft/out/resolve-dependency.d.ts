import { Job } from './node-file-trace';
export declare type FilesToEmit = Array<{
    file: string;
    type: string;
    parent?: string;
    isRealpath?: boolean;
}>;
export default function resolveDependency(specifier: string, parent: string, job: Job, cjsResolve?: boolean): Promise<string | string[]>;
